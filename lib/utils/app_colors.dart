import 'package:flutter/material.dart';

class AppColors {
  // Your custom palette
  static const Color mainAccent = Color(0xFF31511E);
  static const Color textAccent = Color(0xFF112B09);
  static const Color lightAccent = Color(0xFF2C420A);
  
  // Standard colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color background = Color(0xFFF3F4EE); // The splash background we set earlier
}